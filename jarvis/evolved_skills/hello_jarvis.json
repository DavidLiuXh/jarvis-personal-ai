{
  "name": "hello_jarvis",
  "description": "A test skill that prints a greeting message. Use this whenever the user wants to test Jarvis's evolution.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": { "type": "string", "description": "The name of the user to greet." }
    },
    "required": ["name"]
  },
  "executable": "hello_jarvis.py"
}
